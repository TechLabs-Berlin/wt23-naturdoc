/* import Dialog from '@mui/material/Dialog';

function Authenticate(open, handleClose, fullScreen) {
 return (
  <>
   <Dialog open={open} onClose={handleClose} fullScreen={fullScreen}>a</Dialog>
  </>
 );
}
export default Authenticate; */